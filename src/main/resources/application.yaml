spring:
  application:
    name: api-hr

  datasource:
    url: jdbc:postgresql://tamabee.vn:5432/stg_hr_tamabee
    username: quanghiep
    password: avatar
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    clean-disabled: false
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: tamabee.info@gmail.com
    password: aockadwfvzewihdp
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: false
          starttls:
            enable: true
            required: true
        debug: true

server:
  port: 8081

logging:
  level:
    root: INFO
    com.tamabee: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

jwt:
  secret: ${JWT_SECRET:TamabeeHRSecretKeyForJWTTokenGenerationMustBeLongEnough}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION:3600000}
  refresh-token-expiration: ${JWT_REFRESH_TOKEN_EXPIRATION:2592000000}
