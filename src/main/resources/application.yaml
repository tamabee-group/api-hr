spring:
  application:
    name: api-hr

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5433/stg_hr_tamabee}
    username: ${DATABASE_USERNAME:quanghiep}
    password: ${DATABASE_PASSWORD:avatar}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    clean-disabled: false
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: tamabee.info@gmail.com
    password: aockadwfvzewihdp
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: false
          starttls:
            enable: true
            required: true
        debug: true

server:
  port: 8081

logging:
  level:
    root: INFO
    com.tamabee: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

jwt:
  secret: ${JWT_SECRET:TamabeeHRSecretKeyForJWTTokenGenerationMustBeLongEnough}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION:3600000}
  refresh-token-expiration: ${JWT_REFRESH_TOKEN_EXPIRATION:2592000000}

database:
  tunnel:
    enabled: ${DATABASE_TUNNEL_ENABLED:false}
    ssh:
      host: ${SSH_HOST:tamabee.vn}
      port: ${SSH_PORT:22}
      username: ${SSH_USERNAME:root}
      password: ${SSH_PASSWORD:}
    local:
      port: ${LOCAL_PORT:5433}
    remote:
      host: ${REMOTE_HOST:localhost}
      port: ${REMOTE_PORT:5432}
